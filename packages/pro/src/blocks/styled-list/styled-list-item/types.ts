export interface BlockTypes {
    blockID: string;
    itemText: string;
    selectedIcon: string;
    iconColor: string;
    iconSize: number;
    fontSize: number;
    padding: object;
    margin: object;
}

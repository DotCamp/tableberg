import { BlockEditProps } from "@wordpress/blocks";
import { TablebergBlockAttrs } from "../types";

export default function StackRowTable(props: BlockEditProps<TablebergBlockAttrs>) {
    return <div>Tada</div>
}